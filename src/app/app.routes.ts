import { Routes } from '@angular/router';
import { FinderBoxComponent } from './components/finder-box/finder-box.component';

export const routes: Routes = [
    {
        path : '',
        component : FinderBoxComponent
    }
];
